# 🤖 Юридический AI-ассистент (RAG)

Интеллектуальная система для анализа казахстанского законодательства на базе Retrieval-Augmented Generation (RAG): гибридный поиск по статьям и генерация ответов с указанием источников.

## 🧠 Идея проекта
Цель — дать гражданам, бизнесу и специалистам быстрый и точный доступ к нормам казахстанского права в разговорной форме. Вместо поиска по длинным PDF/HTML документам система отвечает на вопросы простым языком и подкрепляет ответы ссылками на конкретные статьи.

Почему это важно:
- **Снижение порога входа**: юридическая информация становится доступной без глубоких знаний терминологии.
- **Экономия времени**: быстрый переход к нужной норме права и ключевым положениям.
- **Повышение точности**: вывод строится на найденных статьях, а не на «голых» языковых моделях.

## 👥 Целевая аудитория и кейсы
- **Граждане**: понимание прав и обязанностей, подготовка к обращениям и заявлениям.
- **Малый и средний бизнес**: проверка базовых юридических вопросов (договоры, трудовые отношения, регистрация и т.д.).
- **Юристы/помощники юристов**: быстрый первичный ресерч по нормам, поиск релевантных статей.
- **Госслужащие/НПО/Журналисты**: навигация по действующим нормам и сравнение формулировок.

Типовые сценарии:
- «Что говорит статья 1 ГК РК?»
- «Какие права имеет собственник имущества?»
- «Какие требования к трудовому договору?»

## 🏗️ Архитектура (обзор)
- **Подготовка данных**: разметка и разбиение документов на статьи/чанки (`preprocess_articles.py`).
- **Индексация**: эмбеддинги и загрузка в Pinecone (`embed_and_index_fixed.py`).
- **RAG-ядро**: гибридный поиск (векторный + BM25), переранжирование, агрегация источников (`rag_system.py`).
- **Интерфейсы**: CLI (`legal_chat.py`) и веб (`web_legal_chat.py` + `templates/legal_chat.html`).
- **Интеграции**: OpenAI для эмбеддингов/генерации, Pinecone для векторного поиска.

Диаграмма потоков (вкратце):
Вопрос → Поиск по индексу → Переранжирование → Сбор контекста → Генерация ответа с цитатами → Возврат пользователю.

## 📄 Данные и источники
- Гражданский кодекс РК, Конституция РК, Трудовой кодекс РК и др. (папка `data/raw/`).
- Обработанные статьи/чанки — в `data/chunks/` с метаданными (номер статьи, источник).
- Возможность расширения набора документов без изменения архитектуры.

## ⚖️ Ограничения и риски
- Модель может ошибаться в формулировках — всегда проверяйте источники.
- Не является юридической консультацией — предназначено для справочно-образовательных целей.
- Актуальность источников зависит от обновления корпуса: при изменениях законодательства необходима переиндексация.

## 🗺️ План развития (дополнительно)
- Добавление новых кодексов и подзаконных актов.
- Улучшение переранжирования и кэширования результатов.
- Расширение UI: фильтры по актуальности, дата изменений, сравнение редакций.
- Многоязычная поддержка (казахский/русский/английский).
- Интеграция с судебной практикой и официальными базами данных.

## 🚀 Быстрый старт

### 1) Требования
- Python 3.10+
- Доступ к OpenAI API
- Доступ к Pinecone (или готовый индекс)

### 2) Установка (локально)
```bash
pip install -r requirements.txt
```

### 3) Настройка окружения
Создайте файл `.env` в корне проекта:
```env
OPENAI_API_KEY=your_openai_api_key
PINECONE_API_KEY=your_pinecone_api_key
PINECONE_ENVIRONMENT=us-east-1
PINECONE_INDEX_NAME=legally-index
```

### 4) Подготовка данных и индексация
```bash
# Разбивка документов на статьи/чанки
python preprocess_articles.py

# Создание эмбеддингов и загрузка в Pinecone
python embed_and_index_fixed.py
```

### 5) Запуск
#### Консольный чат
```bash
python legal_chat.py
```

#### Веб-интерфейс (локально)
```bash
python web_legal_chat.py
```
Откройте: http://localhost:5000

## 🐳 Запуск в Docker

### Dockerfile
В проект уже включён `Dockerfile` (экспонирует порт 5000) и `docker-compose.yml`.

### Сборка и запуск
```bash
# Вариант 1: Docker
docker build -t legally-ai-rag .
docker run --env-file .env -p 5000:5000 -v "$(pwd)/data:/app/data" legally-ai-rag

# Вариант 2: Docker Compose
docker-compose build
docker-compose up -d
```
Откройте: http://localhost:5000

Примечание: в `requirements.txt` используйте пакет `pinecone` (без `pinecone-client`).

## 📚 Возможности
- ✅ Поиск по статьям (гибрид: векторный + BM25)
- ✅ Генерация ответов на основе найденных источников
- ✅ Переранжирование результатов (cross-encoder)
- ✅ Контекстные диалоги (память)
- ✅ Веб-интерфейс с отображением источников

## 📁 Структура проекта
```
practice1/
├── data/
│   ├── raw/                    # Исходные документы
│   └── chunks/                 # Разбитые на статьи/чанки тексты
├── templates/
│   └── legal_chat.html         # Шаблон веб-интерфейса
├── benchmark_results/          # Результаты benchmark'ов
├── rag_system.py               # Ядро RAG
├── rag_factory.py              # Фабрика RAG движков
├── legal_chat.py               # CLI-чат
├── web_legal_chat.py           # Веб-сервер (порт 5000)
├── preprocess_articles.py      # Подготовка данных
├── embed_and_index_fixed.py    # Индексация в Pinecone
├── test_rag.py                 # Базовые тесты RAG
├── benchmark_rag.py            # Основной benchmark
├── benchmark_quality.py        # Тест качества
├── benchmark_load_test.py      # Нагрузочное тестирование
├── benchmark_compare_engines.py # Сравнение движков
├── Makefile                    # Команды для benchmark'ов
├── README_BENCHMARK.md         # Документация benchmark'ов
├── requirements.txt            # Зависимости
├── Dockerfile                  # Docker-образ
└── docker-compose.yml          # Compose-конфигурация
```

## ⚙️ Конфигурация и параметры
Ключевые параметры находятся в `rag_system.py`:
```python
self.top_k_initial = 20
self.top_k_final = 5
self.rerank_threshold = 0.5
```

Смена моделей эмбеддингов/переранжирования также настраивается в `rag_system.py`.

## 🧪 Тестирование

### Базовое тестирование
```bash
python test_rag.py
```

### Комплексное тестирование (Benchmark)
Для детального анализа производительности, качества и стабильности системы используйте набор benchmark'ов:

```bash
# Установка зависимостей
make install

# Полный benchmark
make benchmark

# Отдельные тесты
make performance  # Тест производительности
make quality      # Тест качества ответов
make load         # Нагрузочное тестирование
make compare      # Сравнение движков RAG

# Быстрые тесты
make quick        # Быстрый тест производительности
make all          # Очистка + полный тест
```

**Доступные benchmark'ы:**
- `benchmark_rag.py` - Основной benchmark производительности
- `benchmark_quality.py` - Тестирование качества ответов
- `benchmark_load_test.py` - Нагрузочное тестирование
- `benchmark_compare_engines.py` - Сравнение движков RAG

Подробная документация: [README_BENCHMARK.md](README_BENCHMARK.md)

## 🛠️ Устранение неполадок
**Проблема:** ошибка Pinecone о переименовании пакета (`pinecone-client` → `pinecone`).
**Решение:** удалите `pinecone-client` из `requirements.txt`, оставьте только `pinecone`, пересоберите образ.

**Проблема:** веб-интерфейс недоступен.
- Проверьте порт: приложение слушает 5000
- Убедитесь, что команда запущена без ошибок
- Для Docker/Compose проверьте публикацию порта `-p 5000:5000`

**Проблема:** пустые результаты поиска.
- Проверьте, что выполнены `preprocess_articles.py` и `embed_and_index_fixed.py`
- Убедитесь, что индекс в Pinecone содержит данные

## 📈 Примерные показатели
- ~744 статей в индексе
- 1536-мерные эмбеддинги OpenAI + локальные эмбеддинги
- Гибридный поиск с переранжированием

## 📄 Лицензия
Проект предназначен для образовательных и исследовательских целей.

---

🎉 Готово к использованию! Задавайте вопросы по казахстанскому законодательству.

## 🔀 Выбор движка (Baseline / GraphRAG / LightRAG)

По умолчанию используется базовый движок (`baseline`) из `rag_system.py`.
Для переключения установите переменную окружения `RAG_ENGINE`:

```bash
# baseline (по умолчанию)
export RAG_ENGINE=baseline

# GraphRAG (требует отдельной конфигурации индекса)
export RAG_ENGINE=graphrag

# LightRAG (требует отдельной конфигурации корпуса)
export RAG_ENGINE=lightrag
```

Адаптеры для GraphRAG и LightRAG добавлены (см. `rag_factory.py`),
но требуют вашей конфигурации (инициализация индекса/корпуса, пути, параметры).