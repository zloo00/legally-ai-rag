# 🎯 Benchmark для RAG системы

Комплексный набор инструментов для тестирования производительности, качества и стабильности RAG системы юридических документов.

## 📁 Структура

```
benchmarks/benchmark_rag.py              # Основной benchmark файл
benchmarks/benchmark_quality.py          # Тестирование качества ответов
benchmarks/benchmark_load_test.py        # Нагрузочное тестирование
benchmarks/benchmark_compare_engines.py  # Сравнение разных движков RAG
benchmarks/run_benchmark.py              # RAGAS-оценка качества + метрики цитирования/отказов
benchmarks/benchmark_dataset.json        # Датасет для RAGAS-бенчмарка
Makefile                                 # Удобные команды для запуска
README_BENCHMARK.md                      # Документация
```

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
make install
```

### 2. Запуск полного benchmark
```bash
make benchmark
```

### 3. Запуск отдельных тестов
```bash
make performance  # Тест производительности
make quality      # Тест качества
make load         # Нагрузочное тестирование
make compare      # Сравнение движков
```

## 📊 Типы тестирования

### 1. Benchmark производительности (`benchmark_rag.py`)

**Что тестирует:**
- Время ответа на запросы
- Успешность выполнения запросов
- Статистику системы (количество векторов, размерность индекса)
- Нагрузочные характеристики

**Метрики:**
- Среднее время ответа
- Минимальное/максимальное время ответа
- Стандартное отклонение времени ответа
- Процент успешных запросов
- Количество запросов в секунду

**Запуск:**
```bash
python benchmarks/benchmark_rag.py
```

### 2. Benchmark качества (`benchmark_quality.py`)

**Что тестирует:**
- Релевантность ответов
- Наличие ключевых слов
- Корректность источников
- Соответствие номерам статей
- Соответствие кодам законов
- Длину и полноту ответов

**Метрики:**
- Оценка по ключевым словам (0-1)
- Оценка по источникам (0-1)
- Оценка по номерам статей (0-1)
- Оценка по кодам законов (0-1)
- Оценка по длине ответов (0-1)
- Оценка по количеству источников (0-1)
- Оценка релевантности (0-1)
- Общая оценка качества (0-1)

**Запуск:**
```bash
python benchmarks/benchmark_quality.py
```

### 3. Нагрузочное тестирование (`benchmark_load_test.py`)

**Что тестирует:**
- Производительность под различными нагрузками
- Стабильность системы
- Использование ресурсов (CPU, память, диск)
- Деградацию производительности

**Конфигурации нагрузки:**
- Легкая нагрузка: 1 одновременный запрос, 30 секунд
- Средняя нагрузка: 3 одновременных запроса, 60 секунд
- Высокая нагрузка: 5 одновременных запросов, 90 секунд
- Стресс-тест: 10 одновременных запросов, 120 секунд

**Метрики:**
- Количество запросов в секунду
- Процент успешных запросов
- Среднее/минимальное/максимальное время ответа
- Использование CPU и памяти
- Деградация производительности

**Запуск:**
```bash
python benchmarks/benchmark_load_test.py
```

### 4. Сравнение движков (`benchmark_compare_engines.py`)

**Что тестирует:**
- Сравнение разных движков RAG (baseline, GraphRAG, LightRAG)
- Производительность каждого движка
- Качество ответов каждого движка
- Нагрузочные характеристики каждого движка

**Метрики:**
- Сравнительная таблица производительности
- Сравнительная таблица качества
- Сравнительная таблица нагрузочных характеристик
- Рекомендации по выбору движка

**Запуск:**
```bash
python benchmarks/benchmark_compare_engines.py
```

### 5. Качество с RAGAS и цитированием (`benchmark/run_benchmark.py`)

**Что тестирует:**
- context precision/recall, faithfulness, answer relevancy (RAGAS)
- Точность цитирования (heuristic doc+article match)
- Refusal rate (отказы при нехватке контекста)

**Датасет:**
- `benchmarks/benchmark_dataset.json` — 30 примеров (ru/kz), расширяй до 80–100 для устойчивых метрик.

**Запуск:**
```bash
python benchmarks/run_benchmark.py --dataset benchmarks/benchmark_dataset.json --limit 20
```

## 📈 Результаты

### Структура результатов

Все результаты сохраняются в директории `benchmark_results/`:

```
benchmark_results/
├── benchmark_baseline_20241201_143022.json    # Полные результаты
├── performance_baseline_20241201_143022.csv   # Детали производительности
├── quality_baseline_20241201_143022.csv       # Детали качества
├── load_test_baseline_20241201_143022.json    # Результаты нагрузочного тестирования
├── load_test_details_baseline_20241201_143022.csv
├── engine_comparison_20241201_143022.csv      # Сравнение движков
└── comparison_summary_20241201_143022.csv     # Сводка сравнения
```

### Формат результатов

**JSON файлы** содержат полные результаты в структурированном виде:
```json
{
  "engine": "baseline",
  "timestamp": "2024-12-01T14:30:22",
  "performance": {
    "avg_query_time": 2.5,
    "avg_success_rate": 0.95,
    "performance_results": [...]
  },
  "quality": {
    "avg_keyword_score": 0.8,
    "avg_source_score": 0.9,
    "quality_results": [...]
  },
  "load_test": {
    "queries_per_second": 0.5,
    "success_rate": 0.98,
    "results": [...]
  }
}
```

**CSV файлы** содержат детальные результаты для анализа:
- `performance_*.csv` - детали производительности по каждому запросу
- `quality_*.csv` - детали качества по каждому вопросу
- `load_test_details_*.csv` - детали нагрузочного тестирования
- `engine_comparison_*.csv` - сравнительная таблица движков

## 🔧 Конфигурация

### Переменные окружения

Убедитесь, что настроены следующие переменные:
```bash
export OPENAI_API_KEY=your_openai_api_key
export PINECONE_API_KEY=your_pinecone_api_key
export PINECONE_INDEX_NAME=your_index_name
```

### Настройка тестов

Вы можете изменить параметры тестирования в соответствующих файлах:

**benchmark_rag.py:**
- `test_questions` - список тестовых вопросов
- `iterations` - количество итераций для каждого запроса

**benchmark_quality.py:**
- `quality_questions` - вопросы с ожидаемыми ответами
- Веса для расчета общей оценки качества

**benchmark_load_test.py:**
- `load_configs` - конфигурации нагрузочного тестирования
- `load_test_questions` - вопросы для нагрузочного тестирования

## 📊 Анализ результатов

### Интерпретация метрик

**Производительность:**
- Время ответа < 3 секунд - отлично
- Время ответа 3-10 секунд - хорошо
- Время ответа > 10 секунд - требует оптимизации

**Качество:**
- Общая оценка > 0.8 - отлично
- Общая оценка 0.6-0.8 - хорошо
- Общая оценка < 0.6 - требует улучшения

**Нагрузочные характеристики:**
- > 1 запрос/сек - отлично
- 0.5-1 запрос/сек - хорошо
- < 0.5 запрос/сек - требует оптимизации

### Рекомендации

1. **Если низкая производительность:**
   - Оптимизируйте запросы к Pinecone
   - Уменьшите количество документов в контексте
   - Используйте кэширование

2. **Если низкое качество:**
   - Улучшите качество чанков
   - Настройте параметры поиска
   - Добавьте больше релевантных документов

3. **Если проблемы с нагрузкой:**
   - Масштабируйте систему
   - Оптимизируйте использование ресурсов
   - Добавьте балансировку нагрузки

## 🛠️ Устранение неполадок

**Проблема:** Ошибка инициализации RAG системы
**Решение:** Проверьте переменные окружения и доступность API

**Проблема:** Медленное выполнение тестов
**Решение:** Уменьшите количество итераций или длительность тестов

**Проблема:** Ошибки при нагрузочном тестировании
**Решение:** Уменьшите количество одновременных запросов

**Проблема:** Нет результатов
**Решение:** Проверьте права на запись в директорию `benchmark_results/`

## 📝 Примеры использования

### Запуск быстрого теста
```bash
make quick
```

### Запуск только теста качества
```bash
make test-quality
```

### Очистка и полный тест
```bash
make all
```

### Сравнение движков
```bash
make compare
```

## 🎯 Заключение

Этот набор benchmark'ов поможет вам:
- Оценить производительность RAG системы
- Измерить качество ответов
- Протестировать стабильность под нагрузкой
- Сравнить разные движки RAG
- Получить рекомендации по оптимизации

Используйте результаты для принятия обоснованных решений по улучшению системы!
